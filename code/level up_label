extends Label

var valuer:int = 0

func _process(delta):
	await get_tree().process_frame
	valuer = %Button3333.valuer
	text = "All ball probabilities are
improved by 2% each.
PRICE: " + str (valuer) + "$"
	
