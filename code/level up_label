extends Label

var precio_final:int = 0

func _process(delta):
	await get_tree().process_frame
	precio_final = %Button3333.precio_final
	text = "All ball probabilities are
improved by 10% each.
PRICE: " + str (precio_final) + "$"
