extends Label


func _process(delta):
	text = "requirement: " + str(GameManager.requirement)
	if (GameManager.colisiones_totales > GameManager.requirement):
		GameManager.requirement += GameManager.requirement*2 
		get_parent().get_node("Label2").objetivo_conseguido()
