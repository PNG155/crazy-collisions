extends Label

var precio_final:int = 0

func _process(delta):
	await get_tree().process_frame
	precio_final = get_parent().precio_final
	text = "all ball collisions 
	are multiplied by
	2. PRICE: " + str (precio_final) + "$"
